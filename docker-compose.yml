version: "3"
services:
  frontend:
    build: .
    image: frontend
    build:
       context: .
    container_name: frontend
    networks:
       docker_network:
          ipv4_address: 172.20.0.2
networks:
  default:
    external:
       name: allnetwork
  docker_network:
    external: true
